query getUsers(
  $searchText: String
  $first: Int
  $offset: Int
  $userRole: UserRole
  $subtractUsers: [Int!]
) {
  users(
    searchText: $searchText
    first: $first
    offset: $offset
    userRole: $userRole
    subtractUsers: $subtractUsers
  ) {
    users {
      ...basicUserDetails
    }
    totalCount
  }
}
