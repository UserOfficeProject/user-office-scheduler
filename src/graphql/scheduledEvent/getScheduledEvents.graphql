query getScheduledEvents($filter: ScheduledEventFilter!) {
  scheduledEvents(filter: $filter) {
    id
    bookingType
    startsAt
    endsAt
    description
    proposalBooking {
      status
      proposal {
        id
        title
        shortCode
        proposer {
          firstname
          lastname
        }
      }
    }
  }
}
